# Intent: Add onAutoRegister callback to ChannelOpts

Add `onAutoRegister` optional callback to allow channels to auto-register
groups when the bot is mentioned. This enables the Feishu channel to
automatically register new groups when users @ mention the bot.
